<?xml version="1.0" encoding="utf-8"?>
<extension type="component" version="1.7.0" method="upgrade">
 
	<name>MyFilter</name>
	<!-- The following elements are optional and free of formatting constraints -->
	<creationDate>January 2012</creationDate>
	<author>Juan V. Russell</author>
	<authorEmail>juan64@gmail.com</authorEmail>
	<authorUrl>http://juan64.cjb.net</authorUrl>
	<copyright>Copyleft</copyright>
	<license>GNU GPL</license>
	<!--  The version string is recorded in the components table -->
	<version>0.0.1</version>
	<!-- The description is optional and defaults to the name -->
	<description>This manage a web filter</description>
<!--  
	<update> Runs on update; New in 1.7
		<schemas>
			<schemapath type="mysql">sql/updates/mysql</schemapath>
		</schemas>
	</update>
 --> 
	<!-- Site Main File Copy Section -->
	<!-- Note the folder attribute: This attribute describes the folder
		to copy FROM in the package to install therefore files copied
		in this section are copied from /site/ in the package -->
	<files folder="site">
		<filename>index.html</filename>
		<filename>myfilter.php</filename>
	</files>
 
	<administration>
		<!-- Administration Menu Section -->
		<menu>MyFilter</menu>
		<!-- Administration Main File Copy Section -->
		<!-- Note the folder attribute: This attribute describes the folder
			to copy FROM in the package to install therefore files copied
			in this section are copied from /admin/ in the package -->
		<files folder="admin">
			<!-- Admin Main File Copy Section -->
			<filename>index.html</filename>
			<filename>myfilter.php</filename>
			<!-- SQL files section -->
			<!-- <folder>sql</folder> -->
		</files>
	</administration>
 
</extension>
